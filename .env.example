# Public demo mode never reads provider credentials.
NEXT_PUBLIC_DEMO_MODE=true

# Private deployment gate.
HEADROOM_ACCESS_TOKEN=

# Add read-only provider credentials only after reviewing scopes.
OPENAI_ADMIN_KEY=
ANTHROPIC_ADMIN_KEY=
VERCEL_ACCESS_TOKEN=
SUPABASE_ACCESS_TOKEN=
RESEND_API_KEY=
CLOUDFLARE_API_TOKEN=
